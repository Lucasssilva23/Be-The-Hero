Instalei o chocolatey pelo comando PowerShell e depois "cinst nodejs - lts". 

Json - Estruturas de dados.

API - RestFul 

back-end 

Node.js

front-end

React Native 
Abordagem SPA

Instalando npm init -y
Instalando um micro-frameWoork "express", no backend.
Instalando o framework react "npx create-react-app", no front end.
Teste com método HTTP no "Insomnia".
Instalando pacote nodemon ajuda desgastar em toda vez reiniciar, npm install nodemon -D na pasta backend.
Instalando banco de dados "SQLite", npm install knex, em seguida instalando npm install sqlite3 na pasta backend.
Executando npx knex-migrate:make create_ongs
Executando table npx knex migrate:latest 
Executando npx knex-migrate:make create_incident
Executando npx knex migrate:latest 
Istalando CORS npm install cors 
Google font, font-Roboto
Pacote de icones, feather icons
Instalando pacote de icones no frontend, npm install react-icons
Instalando pacote de rotas no frontend para paginação, npm install react-router-dom
Instalando cliente http, ela chama api no frontend , npm install axios
